export const exampleObject = (title:string, description:string, to:string)=>{
    return {
        title,
        description,
        to
    }
}
export const exampleList = [
    exampleObject("Firebase Authentication","Authentication Process Using Firebase.", "/firebase-auth"),
];